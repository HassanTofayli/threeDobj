export interface PreventParentMouseWheelEvent extends MouseWheelEvent {
    preventParentScroll: boolean;
    axis: number;
}
declare class PreventParentScroll {
    elem: HTMLElement;
    private handler;
    constructor(elem: HTMLElement);
    start(): void;
    stop(): void;
    private onMouseWheel(event);
    private getDelta(event, xAxis?);
    private maybePreventMouseWheel(delta, elemLength, elemScroll, scrollLength, event);
    private scrollIsMaxed(elemLength, elemScroll, scrollLength);
    private scrollIsMinned(elemScroll);
}
export default PreventParentScroll;
